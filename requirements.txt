# ============================================================
# 医疗AI模型系统 - Python 依赖（魔搭 MCP / Gradio 部署用）
# ============================================================
# === ModelScope MCP 托管部署必需 ===
# Gradio 5.29.0（魔搭推荐版本，5.34+/5.42+ 会导致MCP创建失败）
# 注意：gradio[mcp] 会自动安装兼容的 mcp 包，不要重复指定 mcp== 版本
gradio[mcp]==5.29.0

# === 业务依赖 ===
# KnowS API 调用
requests>=2.31

# 环境变量
python-dotenv>=1.0

# ML 推理（科室分类模型加载）
scikit-learn>=1.3
joblib>=1.3

# FastMCP（仅本地开发用，托管部署不需要；会与gradio[mcp]的mcp包版本冲突）
# fastmcp>=3.0  # 已注释：与gradio[mcp]的mcp依赖冲突，Docker构建会失败
